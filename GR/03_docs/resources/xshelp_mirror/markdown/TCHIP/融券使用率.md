```
Value1 = GetField("融券使用率");
Value1 = GetField("ShortSell％");
```

```
Value1 = GetField("融券使用率");
Value1 = GetField("ShortSell％");
```

融券使用的比例。計算公式：融券餘額 / 融資限額 * 100%。

單位是％，例如融券使用率為70%的話，則回傳70。

週頻率以上為統計期間的期底值。
