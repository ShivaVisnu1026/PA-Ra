```
Value1 = GetField("融資使用率");
Value1 = GetField("Pomusingratio");
```

```
Value1 = GetField("融資使用率");
Value1 = GetField("Pomusingratio");
```

融資使用的比例。計算公式：融資餘額／融資限額 * 100%。

單位是％，例如融資使用率為70%的話，則回傳70。

週頻率以上為統計期間的期底值。
