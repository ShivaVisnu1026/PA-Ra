```
Value1 = GetField("投信成本");
Value1 = GetField("SBSCost");
```

```
Value1 = GetField("投信成本");
Value1 = GetField("SBSCost");
```

系統估算的投信持股成本。

計算方式是透過每日投信買張，賣張，以及當日的價格，估算出投信的長期持有成本。

週頻率以上為統計期間的期底值。
