```
Value1 = GetField("融資增減");
Value1 = GetField("pomDiff");
```

```
Value1 = GetField("融資增減");
Value1 = GetField("pomDiff");
```

融資餘額與前一期的異動。數值內容/單位依商品類型而異。

台股商品的數值為融資增減張數。
類股商品（細產業指標、主題指標）的數值則是成分股融資增減張數的加總。
大盤商品的數值則是融資增減金額，單位是元。

週頻率以上為期間累計值。
