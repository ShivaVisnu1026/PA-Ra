```
Value1 = GetField("現增比率");
Value1 = GetField("RatioofCashIncrement");
```

```
Value1 = GetField("現增比率");
Value1 = GetField("RatioofCashIncrement");
```

最新一期現金增資內每千股可認購新股的股數。

例如：如果每千股可以認購20股的話，則數值為20。

可以檢查GetFieldDate("現增比率")是否等於0來判斷是否有現金增資的資料。
