```
Value1 = GetField("主力賣張");
Value1 = GetField("Leadertotalsell");
```

```
Value1 = GetField("主力賣張");
Value1 = GetField("Leadertotalsell");
```

前十五大賣超分點的賣超張數加總。

系統每日統計券商分點對個股的買賣記錄，依照賣超張數(賣出張數 - 買進張數)排序，找出前15大分點，然後把這15個分點對這檔個股的賣超張數加總，計算出主力賣張這個數值。

週頻率以上為期間累計值。
