```
Value1 = GetField("因子分數_實現損益偏態係數");
Value1 = GetField("FactorScore_RDSKEW");
Value1 = GetField("因子分數_偏態係數因子(以實現損益為基準)");
Value1 = GetField("FactorScore_Realized skewness Factor");
```

```
Value1 = GetField("因子分數_實現損益偏態係數");
Value1 = GetField("FactorScore_RDSKEW");
Value1 = GetField("因子分數_偏態係數因子(以實現損益為基準)");
Value1 = GetField("FactorScore_Realized skewness Factor");
```

偏態係數因子(以實現損益為基準)經過過濾因子極端值、流動性不足及交易天數不足的商品後的優化排名分數。
