```
Value1 = GetField("因子分數_最高價動能");
Value1 = GetField("FactorScore_MOM_52W");
Value1 = GetField("因子分數_動能因子(以前52週高價為基準)");
Value1 = GetField("FactorScore_52-week High Momentum Factor");
```

```
Value1 = GetField("因子分數_最高價動能");
Value1 = GetField("FactorScore_MOM_52W");
Value1 = GetField("因子分數_動能因子(以前52週高價為基準)");
Value1 = GetField("FactorScore_52-week High Momentum Factor");
```

動能因子(以前52週高價為基準)經過過濾因子極端值、流動性不足及交易天數不足的商品後的優化排名分數。
