```
Value1 = GetField("GDP比例");
Value1 = GetField("ToGDPRatio");
```

```
Value1 = GetField("GDP比例");
Value1 = GetField("ToGDPRatio");
```

指數市值佔GDP的比例。單位是％，如果佔比是150％的話，則回傳150。

市值佔GDP比例可以用來觀察股市的熱度。計算公式：成分股總市值 / GDP ＊ 100%。

週以上頻率，是取期底值。
