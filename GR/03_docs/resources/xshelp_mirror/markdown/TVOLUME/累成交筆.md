```
Value1 = GetField("累成交筆");
Value1 = GetField("TotalMatchTx");
```

```
Value1 = GetField("累成交筆");
Value1 = GetField("TotalMatchTx");
```

開盤迄今整體市場(扣除權證)的成交總筆數。

交易所盤中會定時公布此資訊。如果是分鐘頻率的話會取得當下最新數值，如果是日頻率的話則是當日最後的數值。
