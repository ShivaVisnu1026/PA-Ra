```
Value1 = GetField("量比");
Value1 = GetField("VolumeRatio");
```

```
Value1 = GetField("量比");
Value1 = GetField("VolumeRatio");
```

當日估計量與昨量的放大比例。

計算公式：估計量／昨量。

這個數值越大表示當日的成交量可能會放大，可以當成是交易熱絡度的一個指標。
