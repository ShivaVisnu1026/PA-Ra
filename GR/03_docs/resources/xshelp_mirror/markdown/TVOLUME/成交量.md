```
Value1 = GetField("成交量");
Value1 = GetField("Volume");
```

```
Value1 = GetField("成交量");
Value1 = GetField("Volume");
```

K棒資料的成交量。

數值單位依照商品類型而異，例如台股的話成交量的單位是張，可是美股的話成交量的單位則是股。

語法上也可以使用Volume來取得同樣的數值。
