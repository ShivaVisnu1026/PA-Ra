```
Value1 = GetField("內含值");
Value1 = GetField("IntrinsicValue");
```

```
Value1 = GetField("內含值");
Value1 = GetField("IntrinsicValue");
```

選擇權的內含值(Intrinsic value)，也就是標的股現價與履約價的差異。

如果是買權的話，計算公式為MAX(標的股現價－履約價，0)。
如果是賣權的話，計算公式為MAX(履約價－標的股現價，0)。

週頻率以上為統計期間的期底值。
