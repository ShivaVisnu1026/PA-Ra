```
Value1 = GetField("買賣權成交量比率");
Value1 = GetField("CPTradeRatio");
```

```
Value1 = GetField("買賣權成交量比率");
Value1 = GetField("CPTradeRatio");
```

同契約所有賣權商品(PUT)的成交量／所有買權商品(CALL)的成交量。

統計範圍包含所有履約價/月份(含週契約)，也包含夜盤商品。
