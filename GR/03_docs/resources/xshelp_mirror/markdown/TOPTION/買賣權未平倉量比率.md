```
Value1 = GetField("買賣權未平倉量比率");
Value1 = GetField("CPOIRatio");
```

```
Value1 = GetField("買賣權未平倉量比率");
Value1 = GetField("CPOIRatio");
```

同契約所有賣權商品(PUT)的未平倉量／買權商品(CALL)的未平倉量。

統計範圍包含所有履約價/月份(含週契約)。
