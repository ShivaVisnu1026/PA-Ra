```
Value1 = GetQuote("均價");
Value1 = GetQuote("AvgPrice");
Value1 = q_AvgPrice;
```

```
Value1 = GetQuote("均價");
Value1 = GetQuote("AvgPrice");
Value1 = q_AvgPrice;
```

當日的平均成交價。也可以使用 GetField("均價", "D") 來表示。

計算方式是累加開盤後每筆成交的價格乘上數量，然後除以累計的成交數量，得出當日平均的成交價。
