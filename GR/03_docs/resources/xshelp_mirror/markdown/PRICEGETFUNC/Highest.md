以最新一筆資料為基準點，輸入要計算的期數，然後計算過去期數的極大值。

Highest函數與 FastHighest 函數的運算方式一致，都是用 Extremes 函數抓極大值。

範例：

```
plot1(Highest(high,5));    //繪製5期最高價的最大值的連線
```

```
plot1(Highest(high,5));    //繪製5期最高價的最大值的連線
```
