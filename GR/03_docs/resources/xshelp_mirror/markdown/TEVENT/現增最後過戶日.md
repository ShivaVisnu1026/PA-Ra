```
Value1 = GetField("現增最後過戶日");
Value1 = GetField("LastTransferDayOfCashIncrement");
```

```
Value1 = GetField("現增最後過戶日");
Value1 = GetField("LastTransferDayOfCashIncrement");
```

最近一次現金增資的最後過戶日。

欄位格式為西元年月日，例如20221101。

可以檢查GetFieldDate("現增最後過戶日")是否等於0來判斷是否有現金增資的資料。
