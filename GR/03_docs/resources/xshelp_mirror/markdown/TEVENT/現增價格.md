```
Value1 = GetField("現增價格");
Value1 = GetField("CashIncrementAmount");
```

```
Value1 = GetField("現增價格");
Value1 = GetField("CashIncrementAmount");
```

最新一期現金增資的發行價格。

可以檢查GetFieldDate("現增價格")是否等於0來判斷是否有現金增資的資料。
