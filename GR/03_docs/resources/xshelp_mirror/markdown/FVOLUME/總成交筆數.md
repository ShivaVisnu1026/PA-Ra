```
Value1 = GetField("總成交筆數");
Value1 = GetField("TotalTransaction");
```

```
Value1 = GetField("總成交筆數");
Value1 = GetField("TotalTransaction");
```

交易時段中的成交總筆數。

週頻率以上為區間累計值。

本統計資訊含一般、零股、盤後定價，不含鉅額、拍賣、標購。
