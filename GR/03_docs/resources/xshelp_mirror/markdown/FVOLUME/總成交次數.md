```
Value1 = GetField("總成交次數");
Value1 = GetField("TotalTicks");
```

```
Value1 = GetField("總成交次數");
Value1 = GetField("TotalTicks");
```

成交的總筆數。

資料來源為交易所盤中所傳送的成交明細的筆數。

如果分鐘頻率的話則為該分鐘統計區間內的成交明細筆數。
