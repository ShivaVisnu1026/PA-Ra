```
Value1 = GetField("上漲量");
Value1 = GetField("UpVolume");
Value1 = GetField("上漲成交量");
```

```
Value1 = GetField("上漲量");
Value1 = GetField("UpVolume");
Value1 = GetField("上漲成交量");
```

這一筆K棒的所有成交內比前一價上漲的成交量的加總。

例如，這一筆K棒內有4筆成交，分別是30元2張，30元3張，31元4張，30元5張，而前一筆K棒的收盤價是29。那麼上漲量=6張，下跌量=5張。

計算方法如下：
