以最新一筆資料為基準點，輸入要計算的期數，然後計算過去期數的第N個極小值。

範例：

```
value1 = NthLowest(2,low,5); //計算近5期次低的最低價
plot1(value1);               //繪製近5期次低的最低價的連線
```

```
value1 = NthLowest(2,low,5); //計算近5期次低的最低價
plot1(value1);               //繪製近5期次低的最低價的連線
```
