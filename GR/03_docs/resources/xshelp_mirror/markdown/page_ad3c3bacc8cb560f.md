xf_WeightedClose是 WeightedClose 函數的跨頻率版本，增加了指定頻率的參數，可以計算指定頻率的WeightedClose值。

範例：

```
plot1(xf_WeightedClose("W"));    //繪製週線加權平均收盤價的連線
```

```
plot1(xf_WeightedClose("W"));    //繪製週線加權平均收盤價的連線
```
