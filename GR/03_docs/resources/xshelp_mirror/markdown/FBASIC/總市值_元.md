```
Value1 = GetField("總市值(元)");
Value1 = GetField("TotalMarketValue");
```

```
Value1 = GetField("總市值(元)");
Value1 = GetField("TotalMarketValue");
```

股票或是指數的市值。股票市值以發行股數 ＊ 收盤價來計算，指數則是成分股的市值加總。單位為元。

週以上頻率，是取期底值。
