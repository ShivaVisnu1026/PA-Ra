```
Value1 = GetField("總市值(億)");
Value1 = GetField("MarketCapin100Million");
Value1 = GetField("總市值");
```

```
Value1 = GetField("總市值(億)");
Value1 = GetField("MarketCapin100Million");
Value1 = GetField("總市值");
```

股票或是指數的市值，換算成億元。股票市值以發行股數 ＊ 收盤價來計算，指數則是成分股的市值加總。

週頻率以上為統計期間的期底值。
