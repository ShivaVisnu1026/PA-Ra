計算在平均值為0、標準差為1的標準常態分配下，數值小於傳入值的累計機率。

本函數利用多項式計算近似值，精確度到小數點以下六位。

範例：

```
value1 = NORMSDIST(1.33);       //1.33的標準常態累加分配函數
plot1(value1);
```

```
value1 = NORMSDIST(1.33);       //1.33的標準常態累加分配函數
plot1(value1);
```
