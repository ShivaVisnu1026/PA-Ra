```
Value1 = GetField("處置開始日期");
Value1 = GetField("DispositionSD");
```

```
Value1 = GetField("處置開始日期");
Value1 = GetField("DispositionSD");
```

最近一次處置期間的開始日期。

欄位格式為西元年月日，例如20221101。

請注意：這個日期可能是「未來」的日期(例如剛公佈處置，可是尚未進入處置期間)，也可能是「過去」的日期(例如已經進入處置，或是已經結束處置)。

可以檢查GetFieldDate("處置開始日期")是否等於0來判斷是否有資料。
