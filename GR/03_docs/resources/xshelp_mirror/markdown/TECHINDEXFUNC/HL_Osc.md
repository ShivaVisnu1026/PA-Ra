高低價擺盪指標（High Low Oscillator）是以當日高點至昨收這段價差佔真實區間的比例來判斷強弱勢。

範例：

```
value1 = HL_Osc;       //計算HL-Osc指標
plot1(value1, "HL-Osc");
```

```
value1 = HL_Osc;       //計算HL-Osc指標
plot1(value1, "HL-Osc");
```
