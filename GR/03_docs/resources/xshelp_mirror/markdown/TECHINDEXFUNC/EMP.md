Empty Line是移動平均線的一種變化，它的數值的計算方式是用3期、6期、12期與24期的移動平均線值取平均後產生的。

計算公式：

EMP = （3期收盤價平均 + 6期收盤價平均 + 12期收盤價平均 + 24期收盤價平均）／4

範例：

```
value1 = EMP;       //計算EMP指標
plot1(value1, "EMP");
```

```
value1 = EMP;       //計算EMP指標
plot1(value1, "EMP");
```
