計算目前K棒與N根K棒之前的差值。

計算公式：

Momentum = 當期價格 - 前N期價格

範例：

```
value1 = Momentum(Close,10);       //計算收盤價10期的運動量指標
plot1(value1, "MTM");
```

```
value1 = Momentum(Close,10);       //計算收盤價10期的運動量指標
plot1(value1, "MTM");
```
