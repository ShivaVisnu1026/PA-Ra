```
Value1 = GetQuote("量比");
Value1 = GetQuote("VolumeRatio");
Value1 = q_VolumeRatio;
```

```
Value1 = GetQuote("量比");
Value1 = GetQuote("VolumeRatio");
Value1 = q_VolumeRatio;
```

當日估計量對比昨日成交量的放大比例。
計算公式為：估計量 / 昨量。

數值大於1時表示今日收盤的成交量可能會比昨日成交量大，數值越大代表放大程度可能越大。
