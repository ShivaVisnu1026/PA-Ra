```
Value1 = GetQuote("累委買筆");
Value1 = GetQuote("CulBidTicks");
Value1 = GetQuote("累計買筆");
Value1 = q_CulBidTicks;
```

```
Value1 = GetQuote("累委買筆");
Value1 = GetQuote("CulBidTicks");
Value1 = GetQuote("累計買筆");
Value1 = q_CulBidTicks;
```

開盤迄今的總委買筆數。也可以使用 GetField("累委買筆", "D") 來表示。

證券市場的數值為扣除權證的統計。
