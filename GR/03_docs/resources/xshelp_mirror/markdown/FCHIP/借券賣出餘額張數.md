```
Value1 = GetField("借券賣出餘額張數");
Value1 = GetField("SBRemain");
```

```
Value1 = GetField("借券賣出餘額張數");
Value1 = GetField("SBRemain");
```

借券餘額張數，也就是到目前為止借券的淨張數。

借券賣出餘額約等於前一日的借券賣出餘額＋當日的借券賣出張數＋當日的借券賣出還券張數。

請注意：上面公式內會加上「借券賣出還券張數」的原因是因為這欄位是負值。

週頻率以上為統計期間的期底值。
