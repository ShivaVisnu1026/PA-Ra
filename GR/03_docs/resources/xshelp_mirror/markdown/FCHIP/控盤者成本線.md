```
Value1 = GetField("控盤者成本線");
Value1 = GetField("CostLine");
```

```
Value1 = GetField("控盤者成本線");
Value1 = GetField("CostLine");
```

系統估計的控盤者的持股成本。

控盤者的資料是每日單一價格成交值大於100萬的買賣記錄。

系統依此資料，估計出控盤者的長期持有成本。
