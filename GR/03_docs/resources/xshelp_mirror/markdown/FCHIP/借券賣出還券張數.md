```
Value1 = GetField("借券賣出還券張數");
Value1 = GetField("SBStcokDiff");
Value1 = GetField("借券賣出庫存異動張數");
```

```
Value1 = GetField("借券賣出還券張數");
Value1 = GetField("SBStcokDiff");
Value1 = GetField("借券賣出庫存異動張數");
```

償還先前借券賣出的張數。

週頻率以上為期間累計值。

請注意：這個欄位使用負值來代表償還的動作，所以如果要計算當日借券賣出餘額的異動的話，請使用借券賣出張數＋借券賣出還券張數。
