```
Value1 = GetField("借券張數");
Value1 = GetField("SBLBorrowing");
```

```
Value1 = GetField("借券張數");
Value1 = GetField("SBLBorrowing");
```

借券的張數。

週頻率以上為期間累計值。

借券數量係指當日投資人透過證交所借券中心或向證券商、證金公司借入之證券數量。這個數量並不一定等於借券放空的數量。
