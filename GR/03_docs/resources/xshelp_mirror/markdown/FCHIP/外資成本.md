```
Value1 = GetField("外資成本");
Value1 = GetField("FBSCost");
```

```
Value1 = GetField("外資成本");
Value1 = GetField("FBSCost");
```

系統估算的外資持股成本。

計算方式是透過每日外資買張，賣張，以及當日的價格，估算出外資的長期持有成本。

週頻率以上為統計期間的期底值。
