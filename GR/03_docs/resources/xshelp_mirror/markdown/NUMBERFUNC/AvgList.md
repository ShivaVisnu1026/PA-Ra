使用AvgList時可以傳入多個數值，數值之間使用逗號分隔，例如：

```
Value1 = AvgList(Open, High, Low);
```

```
Value1 = AvgList(Open, High, Low);
```

上述範例內使用AvgList來計算Typical Price ( (開盤價 + 最高價 + 最低價) / 3 )。

請注意: 如果要計算序列型的數值的平均值的話，則可以使用 Average函數 。
