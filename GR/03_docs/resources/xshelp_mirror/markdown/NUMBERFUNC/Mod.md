範例:

```
Value1 = Mod(10, 2); // Value1 = 0 (可以整除)
Value2 = Mod(10, 3); // Value2 = 1(不能整除，除完後餘1)
```

```
Value1 = Mod(10, 2); // Value1 = 0 (可以整除)
Value2 = Mod(10, 3); // Value2 = 1(不能整除，除完後餘1)
```
